@model IEnumerable<BelleEssense.DATA.Size>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SizeDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SizeDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SizeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SizeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SizeId })
        </td>
    </tr>
}

</table>
