@model IEnumerable<BelleEssense.DATA.Label>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LabelDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LabelDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LabelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LabelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LabelId })
        </td>
    </tr>
}

</table>
